export default function ReadingPage() {
  return (
    <main className="app-shell">
      <section className="app-hero">
        <h1 className="app-title">Reading</h1>
        <p className="app-subtitle">
          Reading mode will live here. Use the right-side nav on desktop or the
          bottom nav on mobile to move between sections.
        </p>
      </section>

      <section className="app-card p-8">
        <p className="text-sm leading-7 text-zinc-600 dark:text-zinc-300">
          This section is ready as a destination for the persistent navigation.
        </p>
      </section>
    </main>
  );
}
